# led-cascade-effect
This project implements a cascade effect using 6 LEDs with Arduino and the millis() function. The LEDs turn on sequentially, creating a continuous running-light effect based on time control.
